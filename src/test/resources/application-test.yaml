quarkus:
  datasource:
    db-kind: h2

  hibernate-orm:
    log:
      sql: true

  otel:
    enabled: false

  # Disable OpenID Connect in test
  oidc:
    enabled: false
    tenant:
      enabled: false
  keycloak:
    devservices:
      enabled: false

  liquibase:
    change-log: db-test.changelog.yaml

# JWT settings
mp:
  jwt:
    verify:
      issuer: https://auth.example.com
      audiences: app.example.com
      publickey:
        location: dev/public-key.pem

smallrye:
  jwt:
    sign:
      key:
        location: dev/private-key.pem

    new-token:
      issuer: ${mp.jwt.verify.issuer}
      audience: ${mp.jwt.verify.audiences}
