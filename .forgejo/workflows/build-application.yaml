name: Build and Bundle

run-name: Build ${{github.ref_type}} ${{github.ref_name}} by ${{github.triggering_actor}}

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

env:
  REGISTRY: codeberg.org

jobs:
  set-version:
    runs-on:
      labels:
        - codeberg-tiny

    outputs:
      fullSemVer: ${{ steps.version_step.outputs.fullSemVer }}
      major: ${{ steps.version_step.outputs.major }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install GitVersion
        uses: https://github.com/gittools/actions/gitversion/setup@v3.1.11
        with:
          versionSpec: '6.0.x'

      - name: Determine Version
        id: version_step
        uses: https://github.com/gittools/actions/gitversion/execute@v3.1.11

  tag-commit:
    runs-on:
      labels:
        - codeberg-tiny

    needs: set-version

    permissions:
      contents: write

    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Tag version
        run: |
          set -e

          git config --global user.name "Forgejo Actions"
          git config --global user.email "actions@forgejo.org"

          git tag -a "v${{ needs.set-version.outputs.fullSemVer }}" "${{ github.sha }}" -m "Version ${{ needs.set-version.outputs.fullSemVer }}"
          git log --format=fuller -1 "v${{ needs.set-version.outputs.fullSemVer }}"

      - name: Push tag
        run: |
          git push origin "v${{ needs.set-version.outputs.fullSemVer }}:refs/tags/v${{ needs.set-version.outputs.fullSemVer }}"

  build-java:
    runs-on:
      labels:
        - codeberg-small

    needs: set-version

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: https://github.com/actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Set version
        run: mvn versions:set -DnewVersion=${{ needs.set-version.outputs.fullSemVer }} -DgenerateBackupPoms=false

      - name: Build with Maven
        run: mvn -B verify package

      - name: Publish Jar
        uses: actions/upload-artifact@v7
        with:
          name: java
          path: |
            target/*.jar
            target/*.properties
            target/quarkus-app

      - name: Update dependency graph
        uses: advanced-security/maven-dependency-submission-action@v3

  bundle-java:
    runs-on:
      labels:
        - codeberg-small

    needs:
      - build-java

    permissions:
      packages: write
      attestations: write
      id-token: write

    env:
      IMAGE_NAME: ${{ github.repository }}-backend

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download application artifact
        uses: actions/download-artifact@v8
        with:
          name: java
          path: target/

      # This step uses [docker/metadata-action](https://github.com/docker/metadata-action#about) to extract tags and labels that will be applied to the specified image. The `id` "meta" allows the output of this step to be referenced in a subsequent step. The `images` value provides the base name for the tags and labels.
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern=v{{version}},value=${{ needs.set-version.outputs.fullSemVer }}
            type=semver,pattern=v{{major}},value=${{ needs.set-version.outputs.fullSemVer }}
            type=edge,branch=main
            type=ref,event=pr

      # Uses the `docker/login-action` action to log in to the Container registry registry using the account and password that will publish the packages. Once published, the packages are scoped to the account defined here.
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # This step uses the `docker/build-push-action` action to build the image, based on your repository's `Dockerfile`. If the build succeeds, it pushes the image to GitHub Packages.
      # It uses the `context` parameter to define the build's context as the set of files located in the specified path. For more information, see [Usage](https://github.com/docker/build-push-action#usage) in the README of the `docker/build-push-action` repository.
      # It uses the `tags` and `labels` parameters to tag and label the image with the output from the "meta" step.
      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: src/main/docker/Dockerfile.jvm
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # This step generates an artifact attestation for the image, which is an unforgeable statement about where and how it was built. It increases supply chain security for people who consume the image. For more information, see [Using artifact attestations to establish provenance for builds](/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
      - name: Generate artifact attestation
        uses: https://github.com/actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
